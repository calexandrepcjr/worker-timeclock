#ifndef SHOW_TODAY_BALANCE_DOT_H /* This is an "include guard" */
    #define SHOW_TODAY_BALANCE_DOT_H /* prevents the file from being included twice. */
                        /* Including a header file twice causes all kinds */
                        /* of interesting problems.*/

    #include <stdio.h>
    #include "../lib/timelord.h"
    #include "../lib/time_info.h"
    #include "../lib/file.h"

    int show_today_balance();
#endif /* SHOW_TODAY_BALANCE_DOT_H */
