#ifndef REGISTER_CURRENT_TIME_DOT_H /* This is an "include guard" */
    #define REGISTER_CURRENT_TIME_DOT_H /* prevents the file from being included twice. */
                        /* Including a header file twice causes all kinds */
                        /* of interesting problems.*/

    #include <stdio.h>
    #include "../lib/timelord.h"

    void register_current_time();
#endif /* REGISTER_CURRENT_TIME_DOT_H */
